fun main(){
   
    // 1. Crie funções para as 4 operações aritméticas básicas
    
    fun somar(valor1: Int, valor2: Int): Int {
	return valor1 + valor2
        
      
}
   println(somar(5, 3))
   
    fun subtrair(valor1: Int, valor2: Int): Int {
	return valor1 - valor2
        
        
}
    println(subtrair(10, 3))
    
    fun multiplicar(valor1: Int, valor2: Int): Int {
	return valor1 * valor2
        
        
    }
    
    println(multiplicar(5, 3))
    
        fun dividir(valor1: Int, valor2: Int): Int {
	return valor1 / valor2
            
        }
        
        println(dividir(5, 10))
}

fun main(){
   
    // 2. Utilizando a estrutura condicional, desenvolva um algoritmo que determine se uma pessoa é maior ou menor de idade

	val idade = 24
    if(idade >= 18) {
        
        println("Parabéns, você é maior de idade!")
        
    } else{
        println("Infelizmente você não é maior de idade!")
    }
}

fun main(){
   
    // 3. Utilizando a estrutura condicional e operadores relacionais, desenvolva um algoritmo que identifique 
    // se um determinado número inteiro é par ou ímpar

val  numero = 4
      if((numero  % 2) == 0)  
         System.out.println("Número Par")
      else 
         System.out.println("Número Impar")
    
}

fun main(){
   
    // 4. Utilizando a estrutura condicional e operadores lógicos, desenvolva um algoritmo que simule um sistema de login

val nome = "Tales Andrade"
    val senha = "123456"
    
    if (nome == "Tales Andrade"){
        
        println("Login efetuado com sucesso!")
        
        
            println("Bem-Vindo")
        
         } else {
        
        println("Erro! Digite as informações novamente ou cadastre-se")
    }
}

fun main(){
    
    // 5. Desenvolva uma função que calcule o IMC de uma pessoa, imprimir o valor do IMC e sua classificação
    
   fun calcularIMC(peso: Double, altura: Double) {
    
    val imc = peso / (altura * altura)
    
    println("Seu imc é: $imc")
    
    if (imc < 18.5) {
      println("Abaixo do peso")
        
    } else if (imc >= 18.5 && imc < 25.0) {
        println("Peso Normal")
        
    } else {
        println("Acima do peso")
        
    }
}
   calcularIMC(75.3, 1.87)

calcularIMC(75.3, 1.6)
}

fun main(){
    
    // 6. Utilizando o loop de repetição for, desenvolva um algoritmo que imprima qualquer tabuada
    
    
    val tabuada = 5
    
for (n in 0..10) {

    println("$tabuada x $n = ${tabuada * n}")

}
}

fun main(){
    
    // 7. Utilizando o loop de repetição for, desenvolva um algoritmo que conte a quantidade de vogais em uma String

	var vogais = 0
    
for (letra in "paralelepípedo") {

    if (letra == 'a' || letra == 'ã') {
        vogais = vogais + 1

    } else if (letra == 'e') {
        vogais = vogais + 1

    } else if (letra == 'i') {
        vogais = vogais + 1

    } else if (letra == 'o' || letra == 'ó') {
        vogais = vogais + 1

    } else if (letra == 'u' || letra == 'ú') {
        vogais = vogais + 1

    }

}

println("Total de vogais: $vogais")
}



